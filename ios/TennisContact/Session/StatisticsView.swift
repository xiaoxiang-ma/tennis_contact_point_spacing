// StatisticsView.swift
// TennisContact — iOS
//
// Per-shot and session-level analytics: contact position, wrist velocity,
// consistency score, and contact scatter plot (Swift Charts).
// See docs/implementation_v3.md Section 2.4 and Task 11

import SwiftUI

struct StatisticsView: View {
    var body: some View {
        // TODO (Task 11): shot list, aggregate metrics, scatter plot, consistency score
        Text("Statistics")
    }
}

#Preview {
    StatisticsView()
}
