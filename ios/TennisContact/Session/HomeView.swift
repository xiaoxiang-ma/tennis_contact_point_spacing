// HomeView.swift
// TennisContact — iOS
//
// Session list screen with "+" button to start a new session.
// See docs/implementation_v3.md Section 2.2

import SwiftUI

struct HomeView: View {
    var body: some View {
        // TODO (Task 12): load sessions from CoreData, display in reverse chronological order
        Text("Home — Session List")
    }
}

#Preview {
    HomeView()
}
