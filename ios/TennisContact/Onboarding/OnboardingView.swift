// OnboardingView.swift
// TennisContact — iOS
//
// Dominant hand selection and camera setup guide (shown once at first launch).
// Stores dominant hand choice in UserDefaults.
// See docs/implementation_v3.md Section 2.1

import SwiftUI

struct OnboardingView: View {
    var body: some View {
        // TODO (Task 4+): dominant hand picker, camera setup guide
        Text("Onboarding")
    }
}

#Preview {
    OnboardingView()
}
